"use strict"

let width = prompt("Write the width of the box", "Width");
let height = prompt("Write the height of the box", "Height");
let length = prompt("Write the length of the box", "Length");

let volume = width * height * length;

alert(`The volume of the box is ${volume}`);